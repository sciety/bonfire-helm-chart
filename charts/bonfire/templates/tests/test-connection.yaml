apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bonfire.fullname" . }}-test-connection"
  labels:
    {{- include "bonfire.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bonfire.fullname" . }}:{{ .Values.service.port }}/dashboard']
  restartPolicy: Never
